# HANDOFF — Лендинг «Ник Меньшов» (для продолжения в новом окне)

> Прочитать первым. Полная сводка состояния, решений, окружения и того, что осталось.
> Дата сводки: 2026-06-11. Деплоя ещё НЕ было (по решению клиента).

## Обновление 2026-06-12 (v10.1 — авторские фото клиента в карусель)
- Клиент прислал архив `~/Downloads/Новая папка с объектами.zip` (19 фото; чат-вставки картинок НЕ дают файлов на диске — только zip/ссылка/Downloads сработали). Отобрал **12 портфолио-кадров**, обработал sips q78 → `site/media/photos/nm-*.jpg` (nm-chandelier, nm-red-room, nm-awards-booth[wide], nm-mixing-white, nm-heart-neon, nm-snarl, nm-raw-prologue, nm-beanie-deck, nm-warehouse, nm-notes-eyes, nm-shades-profile, nm-party-crowd).
- **Порядок в фото-ленте (по просьбе клиента): сначала авторские `nm-*` (12), следом Top15 `photo-*` (12)** = 24 фото. Top15 НЕ удалял (клиент явно просил оставить). Проверено вживую (cached playwright, headless Chromium — preview-MCP скриншот не прорисовывает ленивую фото-ленту, чёрные ячейки): order ок, ваши кадры открывают ленту, 0 битых, overflow 0.
- ⚠️ **Был мисмап исходников и исправлен:** `nm-beanie-deck.jpg` сперва собрался из IMG_4080 (дальний кадр «презентация Визионист») с подписью про шапку — перегенерил из **IMG_4081** (правильный «шапка/бело-сиреневая куртка за пультом»). Теперь файл↔подпись совпадают.
- **Отложены 7 кадров** (бэкстейдж/лайфстайл, не портфолио-уровень): IMG_1346 (пузырчатая плёнка «Top100» на лице), IMG_6183 (тельняшка+ноут днём), IMG_6184 (розовый холодильник, лайфстайл), IMG_6185 (ноут за тёмным передним планом), IMG_1326 (вынос флайт-кейса), **IMG_4080 (презентация «Визионист», дальний план)**, IMG_6174 (двор, бумага на лице). Исходники в `/tmp/nm_zip/Новая папка с объектами/` — добавлю любые по запросу.
- CSS не менялся (лента тянет любые форматы), версия осталась `?v=11`.

## Обновление 2026-06-12 (v10 — секция «Плейлисты» + скрытие «Саунд-дирекции»)
CSS → **`?v=11`**. Превью serverId `e7d59b83-...` (порт 8262). Проверено вживую 1440+390, консоль чистая, overflow 0.

### Новая секция «Плейлисты» (#playlists) — между Портфолио и Райдером ✅
- Данные-driven: модуль `playlists()` в main.js рендерит из массива **`PLAYLISTS`** (сейчас ПУСТ → показывает 2 аккуратных плейсхолдера «скоро» с фирменными чипами Яндекс Музыка/Spotify, пунктирная рамка).
- **Как добавить живой плейлист:** в `PLAYLISTS` вставить объект `{ platform:'spotify'|'yandex', title, embed, height }`, где `embed` — ТОЧНЫЙ src из «Поделиться → Встроить» (Spotify: `open.spotify.com/embed/playlist/ID?theme=0`; Яндекс: `music.yandex.ru/iframe/#playlist/USER/KIND/`). iframe рендерится с `loading=lazy`, `referrerpolicy`, `allow=encrypted-media…`. Примеры закомментированы в коде.
- **ЖДЁМ ОТ КЛИЕНТА:** ссылки/embed-код плейлистов Я.Музыки и Spotify — вставлю в массив, секция оживёт мгновенно.
- CSS: `.pl-grid` 2 кол (≤760 → 1), `.pl-card` hairline+ink-2, чипы тинтуются по `data-platform` (spotify зелёный, yandex жёлтый), `.pl-card--soon` пустое состояние.
- Навигация: «Плейлисты» добавлены в десктоп-меню (после Портфолио) и мобильное (№03).

### Скрыта секция «Саунд-дирекция» (НЕ удалена) ✅
- Введена утилита **`.is-hidden { display:none !important }`** (main.css, рядом с `.accent`).
- Класс `is-hidden` навешен в 4 местах (везде HTML-комментарий «СКРЫТО до доработки»): сама `<section#sound-director>`, ссылка в `.nav-links`, ссылка в `.mobile-menu`, кнопка в `.hero-actions`. Разметка/кейсы/JS целы — модуль `casesDisclosure` просто работает в невидимой секции.
- **Перенумерация (чтобы на деплое не было разрыва):** видимые секции/пункты идут 01–06 без дыр (Обо мне 01, Портфолио 02, Плейлисты 03, Райдер 04, Даты 05, Контакты 06). Скрытая Саунд-дирекция в исходнике сохраняет свой `02`/`.ix 02` (не видно).
- **ЧТОБЫ ВЕРНУТЬ САУНД-ДИРЕКЦИЮ:** убрать `is-hidden` в 4 местах; затем перенумеровать — Саунд-дирекция вернёт себе позицию после «Обо мне», а Портфолио→Контакты сдвигаются +1 (и `.ix` секций, и `num` в мобильном меню). Метки-комментарии помогают найти все точки (`grep -n is-hidden index.html`).
- SEO не трогал: `knowsAbout` всё ещё содержит «саунд-дирекция мероприятий» (услуга остаётся, скрыт лишь раздел на странице).

## Обновление 2026-06-12 (v9.2 — переименование микса)
Сет 02 «SashaKsuuu» → **«Georgia Live Kvevri»** (3 места: sr-title, aria-label, TRACKS в main.js). Файл `set02-sashaksuuu.m4a` НЕ переименовывался (внутреннее имя, не светится в UI). Проверено: строка и плеер показывают новое название, трек играет.

## Обновление 2026-06-12 (v9.1 — субтитры: авторские правки клиента)
about.vtt, теперь **14 cue** (текст финален, NOTE обновлён): cue 1 → «Привет! Меня зовут Никита Меньшов, и меня два.» (запятая моя, клиент дал без неё); cue 10 «кампы»→«кэмпы»; бывший cue 12 разбит на два по таймингу (38.28-40.40 «Они неразрывно связаны.» + 40.40-43.52 «Ведь и зимой и летом, я живу эмоциями, людьми и своим любимым делом.» — пунктуация клиента сохранена как есть), чтобы подпись не вылезала за 2 строки (замерено: ровно 2 на 1440). Старый cue 13 → cue 14. Проверено вживую: 14 cue грузятся, тире нет.

## Обновление 2026-06-12 (v9 — ленты галереи со скроллом + SEO/AI-пакет)
CSS → **`?v=10`**. Превью serverId `0929dba7-7973-4669-bdb1-c2571717297f`. Проверено вживую на 1440 и 390, консоль чистая, overflow 0.

### Галерея → горизонтальные ленты ✅
- **Видео: ВСЕ 8 рилсов** (Я.Диск ревизован через cloud-api: всего 8 файлов IMG_2906-2911 + IMG_4126 + skyeng — все уже были перекодированы в reel1-8; «не все» = в галерее было 5 из 8). Добавлены reel4 (квадрат 1280×1280, класс `.sq`), reel5, reel6; постеры сгенерены (avconvert 0.4с клип → qlmanage → sips).
- **Фото: 12 из 24** (добавлены 7: club-white, festival-wide, street, mic-bw, event-red, studio, portrait-sq; sips q78, суммарно 2.7 МБ).
- **`.pf-strip`**: flex, overflow-x:auto, scroll-snap proximity, тонкий скроллбар (hover=accent), высота ряда clamp(300px,16rem + 12vw,440px), ≤560 clamp(220px,96vw,340px); ширина плитки из aspect-ratio (tall 9/14, wide 16/10, sq 1/1). Старые grid-правила удалены. Композиция: открытие = продающий кадр сегмента, ритм тёплое↔ч/б, портретные перебивки широкими, закрытие = лицо (portrait-sq). Подсказка «Листайте вбок» в pf-note. JS звука работает в лентах без изменений.
- ⚠️ ГРАБЛИ ЗАМЕРОВ: вкладка preview-MCP может быть схлопнута (`innerWidth=0`) → срабатывает мобильный MQ и все замеры мусор. Перед evals — `preview_resize`.

### SEO + AI-видимость ✅
- **Title**: «Ник Меньшов · диджей на свадьбу и корпоратив в Москве · Sound Director · DJ»; description с интентом, регалиями, ценой от 80 000 ₽ и сроком ответа. OG/twitter синхронизированы.
- **JSON-LD → @graph**: Person (@id, award, knowsAbout, alternateName с «Никита») + **ProfessionalService** (telephone, priceRange, areaServed Москва/Россия, 4 Offer с ценами = видимым ценам прайс-борда) + WebSite. Парсится ✓.
- **llms.txt** создан (саммари для AI-поисковиков: кто, услуги+цены, бронирование, ссылки). **robots.txt**: явные Allow для GPTBot, OAI-SearchBot, PerplexityBot, ClaudeBot, Google-Extended, YandexBot и др. + Sitemap. **sitemap.xml**: lastmod 2026-06-12 + image-расширения (og, свадьба, фестиваль).
- **ЧЕСТНО ПРО ТОП-3** (сказано клиенту): on-page максимум сделан, но топ-выдача по «диджей москва» требует: СВОЙ ДОМЕН (github.io субдомен слаб и не принимается Яндекс.Вебмастером как отдельный сайт), обратные ссылки (Top15/Top100 уже дают 2), отзывы, Google Business Profile/Яндекс.Бизнес, время индексации. По бренд-запросу «Ник Меньшов диджей» топ-3 реалистичен быстро; по категорийным — месяцы + домен.

## Обновление 2026-06-12 (v8 — пакет правок клиента: подпись над статами, звук в галерее, плеер всегда виден, 720p)
Preview serverId `5ad7b36f-0305-4df8-aad8-d7903e409276` (порт 8262). CSS → **`?v=9`**. Всё проверено вживую, консоль чистая.
- **Субтитры:** «стал инструктором нескольких школ» — УЖЕ отсутствовал в about.vtt (cue 5 = «сыграл собственную свадьбу.»), ничего не менял.
- **Подпись субтитров над статами:** `.about-cap` перенесена в разметке ВНУТРЬ `.wrap` перед `.stats` и переведена из absolute в ПОТОК (`min-height:2.8em` против дёрганья на смене cue, margin-top clamp, transform translateY). Хак `+84px`/`padding-bottom +108px` (клиренс от плеера) УДАЛЁН за ненадобностью. Проверено: top подписи выше top статов ✓.
- **Плеер сквозной:** автоскрытие при скролле вниз УБРАНО (модуль player: `onScroll()` теперь только снимает `.hidden`). Виден всегда на всех секциях.
- **Портфолио в двух частях:** группы «Видео» и «Фото» с кикерами `.pf-part` (стиль = sets-head: Archivo Expanded капс + hairline + note «Клик включает звук»).
- **Видео галереи СО ЗВУКОМ:** у всех reel*.mp4 подтверждена аудио-дорожка (avmediainfo: Sound 'soun' ×5). Новая логика: ховер = тихий превью (muted), **клик = звук** (unmute+play, маркер ▶→♪, класс `.sound` с акцентом), повторный клик = пауза+mute. **Эксклюзивность звука:** клик по плитке паузит миксы (`#audio`) и глушит другие плитки; запуск миксов глушит звучащие плитки (проверено двусторонне).
- **«налог НПД» → «налог»** в pb-foot (НПД в тексте сайта больше нигде).
- **Лид «Обо мне» переписан (клиент: «менее пафосно, уверенно но не громко»):** было «Я читаю зал и веду его от первой минуты до последнего трека. От камерных вечеров до 6 000 человек.» → стало **«Тонко чувствую аудиторию, [accent]будь передо мной 7 или 6 000 человек.»** Источник тона: собственное описание Ника на top15moscow.ru (meta description: «тонко чувствует аудиторию, будь перед ним 7 или 6 000 человек»). Без тире ✓, 2 строки, overflow 0.
- **Апскейл «Обо мне» (клиент просил ещё дважды):** итоговый `about-hd.mp4` = **1080×1920, 55.8 МБ** (`avconvert Preset1920x1080 --multiPass`, faststart ✓; путь апгрейдов за сессию: 540×960/25МБ → 720×1280/38.5МБ → 1080p). 55.8 МБ < 100 МБ лимита GitHub (будет warning после 50 МБ — ок). При full-bleed 1440 апскейл теперь 1.33x (у 720p был 2x). Десктоп ≥900px получает 1080p, мобила по-прежнему 360×640/13 МБ. Проверено вживую: videoW/H=1080×1920, 13 cue, ошибок нет. Дальше поднимать НЕКУДА разумно: PresetHighestQuality = ре-комп 4K (~150+ МБ, за лимитом), HEVC — нет Firefox.

## Обновление 2026-06-11 (v7 — решения клиента по Council #2 + статичный прайс + СИСТЕМНЫЙ фикс clamp)

### Решения клиента (та же сессия)
- **ИП/НПД:** «возможность работы через ИП, НПД вшит в гонорар» → строка в подвале прайс-борда: «Договор и безнал через ИП, налог НПД уже включён в гонорар».
- **Срок ответа: 15–30 минут** → вшит в 3 места: лид секции «Даты» («Отвечаю лично в течение 15–30 минут»), form-note под кнопкой (первой строкой), лид «Контактов» («обычно за 15–30 минут»).

### Статичный прайс-борд (закрыт P0 маркетинга + P0 ивентщика) ✅
- `.price-board` между shead и book-grid: 4 плитки-кнопки (Свадьба 120k/7ч · Частное/корпоратив 120k/6ч · DJ-сет 80k/2ч · DJ-сет 100k/3ч) + `.pb-foot` (продление/ИП/оговорка). Цены видны ДО формы.
- Клик по плитке подставляет формат в `#f-format` + dispatch change (модуль `priceBoard` в main.js после `pricing`) → панель цены в форме открывается, плитка получает `.on` (inset-полоска accent). Обратная синхронизация по change селекта.
- Стиль — система .stats: hairline-сетка, Prata-цифры clamp(1.4–2.1rem), капс-подписи. ≤760px → 2×2. Проверено вживую 1440/390: клик ✓, .on ✓, панель ✓, overflow 0 ✓.

### ⚠️ СИСТЕМНЫЙ ФИКС: 22 сломанных clamp() по всему main.css
- **Грабли:** `clamp(2rem,1.2rem+2.6vw,3.6rem)` БЕЗ пробелов вокруг `+` — невалидный CSS-калк, декларация молча отбрасывается (Chromium НЕ лоялен). Весь сайт жил на фолбэках: статы 16px вместо 57px, контакты, футер-бренд, margin'ы .shead, mobile-menu и т.д. Вскрылось на новых плитках (.pb-v = 13.3px = дефолт кнопки).
- Фикс: sed `s/([0-9](rem|px|vw|vh|em))\+([0-9.])/\1 + \3/g` по main.css (22 вхождения; минус-вариантов 0; комбинаторы селекторов паттерн не задевает). **CSS → `?v=8`.**
- После фикса (1440): .stat .n 56.6px, .cx .v 38.4px, .fbrand 80px, .shead mb 59px, .about-lead 36.8px. Секции book/about/contacts отсмотрены на 1440+390 — ничего не разъехалось, overflow 0, консоль чистая. **Типографика сайта теперь в ЗАДУМАННОМ масштабе — клиенту стоит взглянуть свежим взглядом.**
- ПРАВИЛО ДЛЯ БУДУЩИХ ПРАВОК: внутри clamp()/calc() всегда пробелы вокруг + и −.

### Council #2: что осталось (нужен контент от клиента)
отзывы (P0, нет ни одного); фраза-мост к ЦА в hero; «что входит в цену / чьё оборудование» (P0 невесты); контекст к названиям сетов; WhatsApp (давать ли); сократить сноуборд-абзац (P2). Кейсы — последний этап (решение клиента).

## Обновление 2026-06-11 (v6 — фото галереи с Top15 + LLM Council #2 + CLIENT PATH)
Текущая сессия. Кейсы — на последний этап (решение клиента). Брони идут на **t.me/nickmenshov** (подтверждено клиентом). Preview serverId `208d87f7-914f-45e6-a52c-0c41539e40b9` (пред. сервер умер во время playwright-скриншотов — preview_start заново).

### Фото в Портфолио — СДЕЛАНО ✅
- Источник: **https://top15moscow.ru/djs/nickmenshov** (страница подтверждена: контакты совпадают). 24 фото скачаны полноразмерно (грабли: в HTML хрефы на `/thumbnails/1x/` — полноразмер по тому же пути БЕЗ `/thumbnails/1x`). Кэш: `/tmp/top15photos/`.
- Отобраны 5 (микс: тепло/вермильон + B&W, форматы под ЦА): `site/media/photos/` — `photo-decks-warm.jpg` (за Pioneer, тёплый свет), `photo-wedding-hall.jpg` (люкс-свадьба, люстры), `photo-stage-bw.jpg` (B&W подиум над толпой), `photo-editorial-bw.jpg` (B&W fashion-торс), `photo-red-wide.jpg` (красный клубный, 1920w — закрывающий wide). Сжаты sips q78, суммарно ~1 МБ.
- Разметка: 4×`<figure class="pf-item tall photo rv">` + 1 wide после видео-плиток; `loading=lazy`, `width/height`, alt по-русски. CSS: `.pf-item.photo{margin:0;cursor:default}` + **тёплый тинт** `::after` (oklch accent /0.07 multiply) — объединяет B&W с тёплым рядом (находка совета). JS галереи фото не трогает (ранний выход без `<video>`).
- `.sets` получил hairline-разделитель + padding-top (стык галереи и Сетов).

### LLM Council #2 (маркетинг + UX/UI + клиент-персона) + CLIENT PATH — ВЫПОЛНЕНО
**CLIENT PATH пройден вживую (превью):** hero CTA→#book ✓ (smooth scroll в скрытой вкладке превью замерзает — артефакт, не баг); формат→автоцена ✓ (120 000 ₽/7ч+продление+оговорка); календарь→выбор даты ✓ (фокус уходит в имя, ячейки div с role=button+tabIndex+Enter/Space — клавиатура ОК); форма→honest fallback ✓ (валидация, honeypot, сообщение с t.me/nickmenshov; в скрытой вкладке буфер недоступен → честный вариант сообщения, как задумано); rider.pdf 200 ✓; контакты-ссылки ✓.
**Применённые фиксы по итогам:** (1) [P1] падеж месяца «19 июнь»→«19 июня» (MONl в main.js теперь родительный, MON остался для заголовка); (2) тёплый тинт фото (выше); (3) разделитель .sets; (4) `.about-body` 60ch→56ch. CSS → **`?v=6`**.
**Отклонено с проверкой (НЕ применять повторно):** «катакана переполняет hero на 390» — ЛОЖЬ (overflowX=0); «pnav плеера скрыт на мобиле» — ЛОЖЬ (display:flex); «кнопка/статус формы под плеером на мобиле» — ЛОЖЬ (зазор 110px+, мерил с видимым плеером); «осветлить верх скрима about» — конфликтует с калибровкой v5 по белому комбезу (читаемость > проявление видео); «tall span4 на 901–1100» — ломает ряды по 4 (4 плитки → 3+1 сирота); «тикер Prata→Archivo» — вид одобрен клиентом, не трогаем без него.
**Передано клиенту на решение (копи/контент, сами не меняем):** статичный прайс-блок ДО формы (P0 маркетинга: цена видна только после выбора формата); фраза-мост к ЦА в hero; отзывы (нет ни одного — P0 обеих персон); «что входит в цену / чьё оборудование» рядом с райдером или прайсом (P0 персоны-невесты); реквизиты/безнал/email-поле для корпоратов (P0 персоны-ивентщика); строка «срок ответа» под кнопкой формы; контекст к названиям сетов (что подходит свадьбе/корпоративу); WhatsApp; сноуборд-абзац сократить (P2).

### Запрос доп. фото клиенту (передать в t.me/nickmenshov)
Нужны для усиления галереи и будущих кейсов: (1) широкий горизонтальный кадр со СЦЕНЫ на большом ивенте (общий план зала/толпы с пультом) — сейчас закрывающий wide клубный; (2) 1–2 кадра со СВАДЬБЫ крупнее (молодожёны/танцпол, тёплый свет) — продаёт сегмент; (3) деловой/корпоративный ивент с брендингом заметным (сцена с логотипом клиента, если права позволяют); (4) качественный портрет анфас для «Обо мне»/прессы; (5) кадр «саунд-дирекция в работе» (за консолью/с командой) для будущих кейсов. Формат: горизонтальные ≥2500px, вертикальные ≥1600px, без сильной обработки.

## Обновление 2026-06-11 (v5 — верификация видео «Обо мне» + фикс подпись/плеер + скрим + VTT без тире)
Текущая сессия. Закрыты пункты 1, 2, 4 из чеклиста v4. Preview serverId теперь `42c538a9-6d84-439c-b57d-8adf5c71817c` (старый осиротевший devserver PID убит, перезапущен через preview_start).

### Видео «Обо мне» — ВЕРИФИЦИРОВАНО вживую ✅
- **Реальное разрешение about.mp4 = 360×640** (НЕ 270×480, как было записано в v4 — avconvert Preset640x480 фитит длинную сторону в 640). 13.1 МБ.
- IntersectionObserver lazy-play работает: `preload=none`, src не тянется до въезда секции; при въезде грузится и играет (readyState 4); пауза вне вью ✓; возврат — играет ✓.
- 13 cue грузятся (devserver отдаёт `text/vtt` ок), субтитры рендерятся в `#aboutCap` синхронно ✓. Лицо видно при `object-position:50% 26%` (десктоп 1440 и мобайл 375) ✓. reduced-motion guard в коде (main.js:84, выход до IO) ✓.
- **«Баг невозобновления» = артефакт preview-MCP**: вкладка превью между вызовами `visibilityState:hidden` → модуль корректно паузит по visibilitychange, а IO в скрытой вкладке не получает кадров (нет рендера) → «не резюмится». В видимом браузере циклы out/in работают стабильно (5/5 после прогрева). НЕ чинить.
- **Резкость: ОТМЕНЕНО ↓** (изначально решили оставить 13 МБ, но клиент попросил апскейл для десктопа — см. подсекцию ниже).

### Апскейл для десктопа (по запросу клиента, та же сессия) ✅
- **Новый файл `site/media/about-hd.mp4` = 540×960, 25.2 МБ** (`avconvert -p Preset960x540 --multiPass` из исходника `/Users/fixed/Movies/xto ya.mov`; multiPass дал лучшее распределение битрейта). moov в начале (faststart), H.264+AAC, 45.6 c. < 100 МБ лимита GitHub ✓, `.gitignore` не исключает ✓.
- **Адаптивная подача (main.js, модуль `aboutVideo`):** при `min-width:900px` И НЕ `navigator.connection.saveData` ставится `v.src='media/about-hd.mp4'`. Мобила/планшеты/Save-Data остаются на лёгком `about.mp4` (360×640, 13 МБ) через `<source>` в разметке.
- ⚠️ **ГРАБЛИ:** менять `src` у `<source>`-элемента ПОСЛЕ парсинга БЕСПОЛЕЗНО — браузер уже выбрал ресурс (resource selection при парсинге, даже с `preload=none`), правка игнорируется. Рабочий способ: **`video.src = ...`** напрямую — перезапускает resource selection, загрузка по-прежнему отложена до play() (lazy-load цел).
- Проверено вживую: десктоп 1440 → `currentSrc=about-hd.mp4`, videoW/H=540×960, 13 cue субтитров на месте, ошибок нет; мобайл 375 → `about.mp4` 360×640 ✓. Визуально на кропах: тату на руке читается линиями (раньше пятно), фактура кожи/ткани появилась. Консоль чистая.

### ФИКС: подпись субтитров коллидила с фикс-мини-плеером
- Плеер: автоскрытие при скролле ВНИЗ, виден при скролле ВВЕРХ и ВСЕГДА при играющем миксе (`.playing`). На ≤560 он во всю ширину → полностью накрывал `.about-cap` (измерено: top подписи 745 = top плеера 745 на 375×812). На средних ширинах (≈560–1300) x-пересечение тоже есть (41px на 1280).
- Фикс в main.css: `.about-cap` поднята на +84px (`bottom:calc(clamp(...) + 84px)`) — выше полосы плеера (макс ~94px с отступами) на ВСЕХ ширинах; `.about.has-video` получил `padding-bottom:calc(var(--section-y) + 108px)` — компенсация, чтобы подпись не съела зазор со статами (зазор 375px: был 4px → стал 36px; 1280: 107px). Проверено на 375/1280: коллизий нет, клиренс 33px/18px при видимом плеере.
- CSS закэш-бащен → **`main.css?v=4`** в index.html.

### ФИКС: скрим — контраст на светлых кадрах
Худший кадр (белый сноуборд-комбез заливает кадр, ~33–45 c) вымывал факты/статы. Углублены стопы линейного скрима `.about-bg::after`: 27%-стоп 30%→**46%** ink (зона лица, щадяще), 60%-стоп 58%→**70%** ink (зона фактов/статов); радиальное прозрачное окно лица 36%→**42%**. Проверено: на белом комбезе текст читается уверенно, лицо на тёмных кадрах не утоплено.

### ФИКС: длинные тире в about.vtt (нарушали правило клиента v2)
В 5 репликах VTT были «—» → переписаны: cue2 «...вечеринками:», cue4 «...Top15 Moscow (15 лучших диджеев Москвы),», cue6 «Вторая часть: зима...», cue12 «...летом я живу музыкой, зимой снегом.», cue13 «И каждый день как первый.» Проверено вживую: hasDash=false, 13 cue.

### Прочее
- `.gitignore` проверен: `site/media/about.mp4` НЕ исключён (исключения только на `concept/media/`) — закоммитится для Pages ✓ (пункт 4 чеклиста v4).
- Скриншоты: после `preview_resize` на десктоп превью отдавал чёрные кадры (рассинхрон композитора, известные грабли) — надёжный путь: `npx playwright screenshot --viewport-size=1440,900 --wait-for-timeout=18000 "http://localhost:8262/?section=about"` (CLI-Chromium, видео доигрывает до нужного сегмента).

### Осталось СЛЕДУЮЩЕМУ окну
1. ~~Подтвердить у клиента ⚠-реконструкции субтитров~~ — **ПОДТВЕРЖДЕНО клиентом 2026-06-11** (cue 12, 13 + формулировки без тире). Субтитры финальны, NOTE в about.vtt обновлён.
2. От клиента: контент кейсов саунд-дирекции, фото портфолио, креды бэкенда (CP3), «ок» на деплой (см. §9).
3. Опциональный polish из v3 (apple-touch-icon PNG, WebP-постеры и т.д.) — не блокеры.

## Обновление 2026-06-11 (v4 — видео «Обо мне» + субтитры + печать + тикер)
Текущая сессия. Дев-сервер теперь **`tools/devserver.py`** (порт 8262): `Cache-Control: no-store`, поддержка **Range (206)**, MIME `audio/mp4` для `.m4a` и **`text/vtt` для `.vtt`**. CSS закэш-бащен: в `index.html` ссылка `main.css?v=3` (любой браузер тянет свежий CSS). ⚠️ Preview-MCP кэшировал старый CSS — для проверок я инъектил свежий `<link ...?v=ts>` через `preview_eval` (см. приём ниже). Preview-MCP serverId `eb8c807a-c276-4efd-99e2-0ff46188f341`, **периодически дрейфует на localhost:4173** (чужой проект) — навигировать обратно на 8262 в каждом eval, гард `if(location.host!=='localhost:8262')`.

### Видео «Обо мне» — СДЕЛАНО (вёрстка/код), НЕ ВЕРИФИЦИРОВАНО вживую
Закрыл отложенную задачу из §«В РАБОТЕ». Исходник `/Users/fixed/Movies/xto ya.mov` оказался **монтажом**: DJ под красным светом → говорящая голова в чёрном худи (~16 c) → сидит в сноуборд-комбезе (24/40 c) → склон (32 c). **Лицо — горизонтально по центру, в верхней трети** портретного кадра (сидячие планы).
- **Транскод:** `avconvert -p Preset640x480` → **`site/media/about.mp4` = 270×480, 13.1 МБ**. (Пробовал 720p=36 МБ и 540×960=24 МБ — оба тяжелы; **24 МБ ВЕШАЛИ рендерер preview** при autoplay. 270×480 за скримом+brightness 0.6 приемлемо, но **резкость лица под вопросом** — см. «решить».)
- **Постер:** `site/media/poster-about.jpg` (46 КБ) — кадр лица с 16-й секунды (`qlmanage -t` по 0.4-с клипу через `avconvert --start/--duration`).
- **Разметка** (`index.html`, секция `#about` получила класс `has-video`): `<div class="about-bg">` → `<video id="aboutVid" muted loop playsinline preload="none" poster width="540" height="960">` + `<source src="media/about.mp4">` + `<track id="aboutTrack" kind="subtitles" srclang="ru" src="media/about.vtt" default>`. Перед `</section>` — `<p class="about-cap" id="aboutCap">` (оверлей субтитра).
- **CSS** (`main.css`, блок после `.stats`): `.about-bg video{object-fit:cover; object-position:50% 26%; filter:grayscale(.16) contrast(1.05) brightness(.6) saturate(.92)}` (центровка по лицу — bias вверх); скрим `.about-bg::after` (linear тёмный верх/низ + radial); `.about-cap` — редакторская подпись Prata, низ-центр (в нижнем padding секции, ниже статов), `.show` = fade-in.
- **JS** (`main.js`, модуль `aboutVideo`): **IntersectionObserver** — `preload=none`, видео грузится+играет ТОЛЬКО когда `#about` въезжает в вьюпорт (не бьёт по LCP), пауза вне вью и при `visibilitychange`. **Свои субтитры**: `textTracks[0].mode='hidden'` (гасит дефолтный `::cue`), `cuechange` → текст в `#aboutCap`. **reduced-motion → только постер, без autoplay**.

### Субтитры — клиент прислал TTML, сконвертировал в VTT
Файл клиента: **`/Users/fixed/Movies/Timeline 1_Subtitle 1.ttml`** (ASR-расшифровка). Конвертация → **`site/media/about.vtt`**:
- **Тайминги перебазированы −1:00:00** (в TTML отсчёт от `01:00:00.000` = видео 0:00; `ttp:timeBase="media"`).
- ASR был **по-словно нарезан и с ошибками**; пересобрал в **13 читаемых фраз**, поправил явные мисхиры: «Никит Миншов»→**Никита Меньшов**, «ТОП 15 Москов»→**Top15 Moscow**, «инстинктом нескольких тупо»→**инструктором нескольких школ**, «плеймбарь… с этой»→**как диджей**.
- ⚠️ **РЕКОНСТРУКЦИИ — подтвердить у клиента** (ASR был сильно искажён): cue 12 `00:38.28→43.52` «Они неразрывно связаны: летом я живу музыкой, зимой — снегом» и cue 13 `00:44.20→45.24` «И каждый день — как первый». Также косвенно cue 3/cue 5 (см. выше).

### Печать (seal) — переделана + ВРАЩЕНИЕ ВЕРНУЛ (клиент передумал)
⚠️ **ОТМЕНЯЕТ решение v2 §7** («печать статичная»). Клиент в этой сессии: «Верни анимацию на круг».
- **Текст центрирован**: добавлен `text-anchor:middle` к `.seal text` (был баг — без него текст СТАРТОВАЛ в 12/6 часов и уезжал вбок: «ЧЛЕН ЖЮРИ» сваливалась вправо, «TOP100 AWARDS» переполняла дугу).
- **Одинаковый размер шрифта на обеих надписях**: `font-size:12px` + `letter-spacing:2.5px` у ОБЕИХ (`.t-top`/`.t-bot`). (Промежуточно пробовал `textLength` — дал кривые интервалы, «поплыло»; и разные letter-spacing — клиент счёл «разный размер». Итог — единые 12px/2.5px.) Капитель кириллицы и латиницы равна (13px, мерил плоским текстом).
- **Вращение**: `@keyframes sealspin{to{transform:rotate(1turn)}}` на `.seal svg` (22s linear infinite, `transform-origin:center`). **音 НЕ вращается** — он отдельный слой `.cn` ВНЕ `<svg>`. Ховер: `.seal:hover svg{animation-duration:9s}` (ускорение) + `.seal:hover{transform:scale(1.05)}` (подъём). **reduced-motion гасит** (`.seal svg` в блоке `animation:none`). Проверено матрицей: на 25%/50% цикла = ровно 90°/180°.

### Тикер
- **Tinkoff → T-Банк** во всех 3 местах (тикер ×2 + список клиентов в кейсах `.res`).
- **Все бренды тикера КАПСОМ** через CSS `text-transform:uppercase` на `.ticker span` (исходный текст читаемый: «Яндекс», «Setters» → рендер ЯНДЕКС, SETTERS, T-БАНК…).

### Осталось сделать СЛЕДУЮЩЕМУ окну (приоритет)
1. **ВЕРИФИЦИРОВАТЬ видео «Обо мне» вживую** (не тестировал — 13 МБ versionнабор не грузил в preview): открыть 8262, проскроллить к `#about` → IntersectionObserver должен запустить видео (`preload=none`); проверить: (а) видео играет и **лицо видно** при `object-position:50% 26%` (подправить % если лицо срезано), (б) **субтитры рендерятся в `#aboutCap`** синхронно (track грузится, `text/vtt` отдаётся ок), (в) скрим даёт читаемость текста «Обо мне», (г) подпись субтитра не наезжает на `.stats`, (д) reduced-motion = постер. Приём против кэша CSS в preview: `preview_eval` → снять старый `<link main.css>`, добавить `assets/css/main.css?v='+Date.now()`.
2. **Решить резкость/вес видео**: 270×480 за скримом может быть мыльным на лице. Нет пресета avconvert между 270×480 и 540×960. Варианты: оставить 13 МБ; ИЛИ поднять до 540×960 (24 МБ) — тогда обязательно lazy-load (уже есть) + проверить, не вешает ли реальный браузер (preview вешало, но это артефакт MCP). Триминг длительности ради качества НЕЛЬЗЯ — субтитры привязаны к полным 45.6 c.
3. **Подтвердить у клиента ⚠️-строки субтитров** (cue 12, 13; см. выше) + поправить any мисхиры.
4. Убедиться, что `about.mp4` НЕ исключён `.gitignore` (нужен в репо для Pages); 13 МБ ок.

## Обновление 2026-06-11 (v3 — миксы в плеере + LLM Council фиксы)
Всё проверено вживую (Preview MCP / Chromium): плеер играет, перемотка работает, консоль чистая.

### Миксы добавлены (сквозной плеер стал РЕАЛЬНЫМ)
- Раньше `#player` был **бутафорией** (только тоггл класса, без `<audio>`, мёртвые кнопки prev/next, фейковый прогресс `width:34%`). Теперь — полноценный плейлист-плеер.
- **3 микса клиента** перекодированы в веб-AAC и лежат в `site/media/mixes/`:
  - `set01-summer.m4a` ← «Nick Cherdak - Your memories. Summer.mp3» (1:01:36) → «Your Memories · Summer / Melodic · Sunset»
  - `set02-sashaksuuu.m4a` ← «SashaKsuuu.mp3» (59:51) → «SashaKsuuu / House · Live»
  - `set03-house-afro.m4a` ← «house + afro new.mp3» (1:48:41) → «House + Afro / Afro House»
- **Транскод:** `afconvert -f m4af -d aac -s 0 -b 112000` (CBR 112k, VideoToolbox). ⚠️ ГРАБЛИ: `-s 3` (VBR)+`-q 127` ИГНОРИРУЕТ `-b` и кодит на ~130k → файл вылез за 100 МБ. CBR (`-s 0`) даёт предсказуемый размер. Итог 50/49/**88** МБ — все < 100 МБ (хард-лимит GitHub на файл). moov-атом в начале (offset 32) → faststart, перемотка ОК.
- **Плеер (main.js модуль `player`)**: массив `TRACKS`, play/pause, prev/next (с заворотом), клик-перемотка + клавиши (←/→ 2%, Home/End, Enter/Space), таймкоды (`#plCur`/`#plDur`, tabular-nums), `#pp` иконка/aria синхронятся с РЕАЛЬНЫМИ событиями `audio` (play/pause/ended), автопереход на следующий по `ended`. `<audio id="audio" preload="none">` — src ставится только при первом действии (нет качания 186 МБ на загрузке). Доступность: seek = `role="slider"` + aria-valuenow/valuetext + клавиатура.
- **Новая секция «Сеты»** в `#portfolio` (под галереей, без правки nav): 3 редакторские строки `01 · название · жанр · длительность · ▶`, клик → грузит трек в сквозной плеер. Активная строка подсвечивается (`.active`/`aria-current`).
- **Плеер переоформлен** (по совету дизайн-совета): убран «спотифай-пузырь» (radius:999px + accent-glow) → плоская консоль-полоса (radius:5px, hairline, blur). CTA-пульс кольца гаснет после первого запуска (`.played`). eq-бары теперь `transform:scaleY` (композитор-френдли), не `height`.
- **MIME-грабли (локально):** Python `http.server` отдаёт `.m4a` как `audio/mp4a-latm` (Chromium может не играть) и БЕЗ Range (нет перемотки). Для теста поднимал свой сервер: правильный `audio/mp4` + Range 206 (`/tmp/rangesrv.py`). **GitHub Pages отдаёт `audio/mp4` + Range из коробки** → на проде ок.

### LLM Council (4 рецензента: код / a11y / перф+SEO / UX-дизайн) — применено
- **P0 showreel-дедлок**: `busy` не сбрасывался при ошибке/зависании видео → весь фон замерзал. Добавлены error/stalled + хард-таймаут 6с + гард `isFinite(duration)`.
- **prefers-reduced-motion**: шоурил теперь НЕ запускается (стоп-кадр на постере), eq/пульсы/анимации отключены.
- **Перф**: hero-видео `#v0` preload=`metadata`, `#v1` preload=`none`+poster, оба с `width/height` (CLS); портфолио-видео получили `width/height`. Noto Serif JP **сабсетнут** через `&text=` (только 音 日本語 ニック・メンショフ → ~10 КБ вместо ~300 КБ), шрифты разбиты на 2 `<link>`.
- **SEO**: добавлены `twitter:title`/`twitter:description`, **JSON-LD `Person`** (sameAs соцсети/регалии), `sitemap.xml` + `Sitemap:` в robots.txt.
- **a11y**: skip-link, `.seal:focus-visible`, aria-label на днях календаря (свободно/занято), `aria-invalid` на полях формы, фокус в мобменю при открытии + возврат на burger по Esc.
- **Календарь**: forward-навигация ограничена +12 мес, кнопки prev/next `disabled` на границах; «занято» = вермильон-точка вместо `line-through` (читается как «бронь», не «сломано»); убран двойной accent на выбранной дате.
- **Форма**: у `<option>` форматов добавлен `value` (раньше формат ТЕРЯЛСЯ в заявке Telegram); честное сообщение если буфер недоступен (раньше всегда «скопировано»); hover/`aria-invalid` стили полей.
- **Прочее**: nav+player в один rAF-scroll-хендлер с null-гардами; tap-safe портфолио (pointerType touch); контакты-оверлей 56%→70% (контраст над видео).

### Осталось (предфинал, требует клиента/опционально)
- **Видео «Обо мне» + субтитры** — НЕ начато в этой сессии (см. ниже §«В РАБОТЕ»). Блокер: текст субтитров от клиента (STT нет, аудио не слышу).
- Опционально (polish, не блокеры): apple-touch-icon PNG 180×180 (сейчас SVG, iOS игнорит); WebP-постеры; бенто-иерархия в `.stats`; реконсиляция дрейфа токенов в DESIGN.md; тиринг акцентов в тикере. 
- Названия/жанры миксов в плеере — поставил по именам файлов; уточнить у клиента, если нужны другие.

## Обновление 2026-06-11 (v2 — правки клиента по секциям)
Сделано в этой сессии (всё проверено в превью, консоль чистая, мобайл ок):
1. **Раздел 02 переименован** «Саунд-директор» → **«Саунд-дирекция»** (nav, мобменю, H2). В теле текст про профессию оставлен корректно.
2. **Layout раздела 02 выправлен**: слева утверждение (акцентная линия) + CTA снизу, справа поясняющий текст; на мобиле стек. Пустой провал убран.
3. **Кейсы вынесены за кнопку** «Смотреть кейсы» (disclosure, `#casesToggle`/`#casesPanel`), вместо уродливой hairline-сетки — чистые карточки. Контент кейсов финализируем позже (CP2).
4. **Убраны ВСЕ длинные тире (—)** из прозы во всех файлах: index.html, main.js (tooltip/сообщение формы), main.css, **rider.html**, **og.html**. Заменено на «:», «,» или «·». Диапазоны «90–100» (en-dash) оставлены. **rider.pdf и og.jpg перегенерированы** (`npx playwright pdf/screenshot`, chromium-1223).
5. **Контакты**: чёрные плашки убраны → секция прозрачная, сквозь неё проступает шоурил (затемнение + верх/низ-фейд для читаемости), контакты — редакторские строки с акцентной стрелкой и ступенчатым появлением.
6. **Сноуборд** (в «Обо мне»): новый текст — инструктор высшей категории + основатель образовательной организации, ссылка **snowsystem.pro**.
7. **Печать Top100 переделана**: статичная (не вращается), сверху арка «ЧЛЕН ЖЮРИ», снизу «TOP100 AWARDS» (вермильон), 音 в центре, ссылка на жюри, aria-label. SVG: `#sealTop`/`#sealBot` арки.
8. **Портфолио → фото+видео галерея**: подписи убраны, выровнено по краям (широкий кадр на всю ширину + ряд из 4 портретных, span 3), мини play-значок (`.pf-mark`) на видео, готово принимать `<img>`. Яркость плиток в покое поднята (постеры были тёмные).
9. **Бронирование — автоприайс по продукту** (`#f-format` data-атрибуты + `#pricePanel`, модуль `pricing` в main.js): Свадьба до 7ч **120 000 ₽**; Частное/корпоратив до 6ч **120 000 ₽**; DJ-сет до 2ч **80 000 ₽**; DJ-сет до 3ч **100 000 ₽**; продление везде **+20 000 ₽/час**. Оговорка: «Цены ориентировочные. Точные условия обсуждаем в переписке.» Цена попадает и в текст заявки в Telegram.

**Жду от клиента (см. §9):** видео для «Обо мне», контент кейсов саунд-дирекции, фото для галереи портфолио.

## В РАБОТЕ — видео для «Обо мне» (старт 2026-06-11, не завершено)
Клиент прислал видео и поставил задачу:
> «Видео для ОБО МНЕ, сделать на весь экран страницы обо мне, отцентровать так, чтобы было видно лицо. Добавить субтитры.»

**Исходник:** `/Users/fixed/Movies/xto ya.mov` — **уже H.264** (`avc1`, НЕ HEVC), **2160×3840 портрет (9:16)**, **45.6 c**, 25 fps, видео ~44.6 Mbit/s, **звук LPCM стерео 48 kHz**, размер **~263 МБ**. Транскодить обязательно (тяжёлый 4К).

**План (не начат):**
1. **Транскод** в веб-H.264 MP4 (фоновое muted-autoplay-loop): уменьшить до ~1080×1920 (или меньше), сильно сжать — цель единицы МБ. Инструмент — `avconvert` (VideoToolbox), т.к. нет ffmpeg/Chrome (см. §6). Также **постер-кадр** с лицом по центру (`qlmanage -t` + `sips` или кадр из avconvert). Положить в `site/media/` (напр. `about.mp4`, `poster-about.jpg`). `.gitignore` уже исключает `IMG_*`/`long_*` — добавить правило, чтобы `about.mp4` коммитился (или хранить отдельно, если будет крупным).
2. **Вёрстка:** полноэкранный фон секции `#about` — `<video autoplay muted loop playsinline poster>`, `object-fit:cover`, **`object-position` подобрать так, чтобы было видно лицо** (видео портретное 9:16, секция широкая — лицо центрировать). Скрим для читаемости текста.
3. **Субтитры:** ⚠️ **Я НЕ слышу аудио и нет STT — нужен текст реплик от клиента** (произносимый русский текст + примерные тайминги, или просто строки — раскидаю равномерно по 45.6 c). Механика: `<track kind="subtitles" src="about.vtt">` в режиме hidden + JS-рендер `cuechange` в стилизованный оверлей (типографика сайта Archivo, низ-центр, лёгкая подложка) — НЕ дефолтный `::cue`. Создать `site/media/about.vtt` (плейсхолдер до текста).
4. **Открытый вопрос по layout (уточнить или решить):** видео как фон ПОД всем текстом «Обо мне» (текст+факты+статы поверх со скримом) ИЛИ отдельная полноэкранная видео-панель (лицо+субтитры), а существующий текст «Обо мне» — ниже. Склоняюсь к фону под всем разделом («на весь экран страницы обо мне»).

**Последнее действие:** проинспектировал исходник (avmediainfo) — H.264 2160×3840 45.6c со звуком. Дальше: транскод + вёрстка + запросить текст субтитров.

---

## 1. Что это
Персональный одностраничный лендинг **Ник Меньшов (Nick Menshov)** — **Sound Director · DJ**.
Цель: имидж + портфолио + бронирование дат. Хостинг — **GitHub Pages** (чистая статика).
Исходный промт: `…/outputs/PROMPT_dj-nick-menshov-landing.md` (есть чекпоинты CP1–CP4, LLM Council, Playwright).

## 2. Где что лежит
```
/Users/fixed/nick-menshov-landing/
├── site/                      ← ДЕПЛОЙ-ПАПКА (Pages публикует ЕЁ)
│   ├── index.html             ← все секции
│   ├── rider.html             ← печатная версия райдера (из неё собран PDF)
│   ├── og.html                ← исходник OG-картинки (не нужен в проде, можно удалить)
│   ├── favicon.svg
│   ├── .nojekyll  robots.txt
│   ├── assets/css/main.css    ← дизайн-токены + ВСЕ стили
│   ├── assets/js/main.js      ← шоурил, плеер, навигация, reveal, календарь, форма
│   └── media/                 ← reel1..8.mp4, poster*.jpg, og.jpg, rider.pdf
├── concept/concept.html       ← черновой hero-концепт (НЕ деплоится), concept/media/* — исходное видео
├── DESIGN.md                  ← style guide (актуальный)
├── CONTENT.md                 ← тексты, регалии, копи + текст раздела «Саунд-директор»
├── README.md
├── HANDOFF.md                 ← этот файл
└── .github/workflows/deploy.yml
```
Дев-серверы (в `~/.claude/launch.json`): **`menshov-site`** → порт **8262** (site/), `menshov-concept` → 8247 (concept/).
Запуск: `cd site && python3 -m http.server 8262` → http://localhost:8262

## 3. Статус по чекпоинтам
- **CP1 (концепт) — APPROVED.** Прошёл ~6 итераций (см. §5).
- **Вёрстка всех секций — ГОТОВО**, адаптив 320–1920, превью показано по секциям.
- **QA / LLM Council — ВЫПОЛНЕНО**, P0/P1 исправлены (§7).
- **CP3 (креды бэкенда) — НЕ выдано.** Бэкенд брони отложен (решение клиента «сначала фронт»).
- **CP4 (деплой) — НЕ сделан.** Клиент выбрал «пока не деплой».

## 4. Дизайн-система (LOCKED) — детали в DESIGN.md
- Направление: **modern fashion / editorial luxury**, палитра **«Vermilion Noir»**.
  `--fg` ivory `oklch(96% .01 85)`, фон `--ink oklch(15% .012 50)`/`--ink-2/3`, акцент `--accent oklch(67% .17 32)` (вермильон).
- Шрифты: **Prata** (дисплей/имя/H2), **Archivo** (+Expanded) (UI/текст/лейблы), **Noto Serif JP** (глиф 音).
- Wordmark: **«Ник Меньшов» кириллицей** + катакана **ニック・メンショフ** (мельче, Noto Serif JP, **вермильон — по прямому требованию клиента**, перебивает правило style-guide «имя одним цветом»).
- Приёмы: полноэкранный шоурил-фон, вращающаяся печать «ЧЛЕН ЖЮРИ · TOP100 AWARDS» (центр 音), бегущий тикер клиентов, вертикальный текст, hairline-сетка, grain, сквозной мини-плеер.

## 5. Хронология решений клиента (чтобы не откатить)
- Палитра: пробовали lime/amber → **Vermilion Noir (A)** финал.
- Эстетика сменилась со «сдержанного японского минимализма» на **modern fashion**, видео **на фоне** (не в рамке).
- Роль = **Sound Director**, «международный», **с 2017** (НЕ «6 лет»). **Жанры убрать. Кол-во стран убрать.**
- Языки: **EN C2 · Японский (日本語) базовый** (слово «Японский» по-русски, 日本語 в скобках).
- Имя «Меньшов» — **без цветного выделения**. «Est. 2017» — убрать. «Nº 01» — убрать.
- Регалии: **член жюри Top100 Awards** (https://top100awards.ru/jury/menshov) · **участник Top15 Moscow** (https://top15moscow.ru/djs/nickmenshov).
- «Сеты» → **«Портфолио»**. Добавлен раздел **«Саунд-директор»** с кейсами (текст в CONTENT.md).
- Кнопки hero: «Забронировать дату» (вермильон) + **«Саунд-дирекция»** (пыльный чёрный).
- **Сквозной мини-плеер** сетов (миксы клиент выдаст позже).
- Тикер клиентов: Яндекс, Tinkoff, **SBER, Setters, VK**, Feerique, MTC, **STAY, PLUS1, REDDAY, BONWEDDINGS** (убраны WeDo, «Для Двоих»).
- Тэглайн (текст клиента): **«Формирую культуру музыкального опыта для мероприятий, брендов и людей.»** (без тире).
- **Убирать «AI-якоря»** (генеративный тон, лишние клише) в текстах и визуале.

## 6. Окружение и приёмы (ВАЖНО — грабли)
- **Нет системного ffmpeg и Google Chrome.** Playwright-ffmpeg НЕ читает HEVC (iPhone-видео).
- **Видео-транскод:** macOS **`avconvert`** (VideoToolbox) — читает HEVC, режет (`--start/--duration`), кодит в H.264. Постеры/превью кадров — **`qlmanage -t`** + `sips`. ffmpeg-бинарь Playwright: `~/Library/Caches/ms-playwright/ffmpeg-1011/ffmpeg-mac` (минимальный, для mp4 не годится).
- **Шоурил** = 8 клипов H.264 из Я.Диска (public_key `HwXuAkT3v2b2Sw`), кроссфейд 2 слоёв `<video>` в main.js. (Длинный `IMG_4126` исключён — статичный портрет не по теме. `skyeng.mov` → reel7/reel8.)
- **PDF/OG** генерим через **`npx playwright screenshot`/`pdf`** (кэш-Chromium, НЕ канал chrome). rider.pdf ← rider.html; og.jpg ← og.html (1200×630).
- **Скриншоты секций:** preview-пане МАСШТАБИРУЕТ высокие страницы (секция выглядит крошечной). Надёжно — `npx playwright screenshot --full-page --viewport-size=1440,900 "http://localhost:8262/?section=<id>"`. В main.js есть debug-изолятор **`?section=<id>`** (hero|about|sound-director|portfolio|rider|book|contacts) — прячет прочие секции.
- Playwright **MCP** настроен на канал `chrome` (не установлен) → MCP-скриншоты не работают; использовать preview-MCP (Chromium) ИЛИ playwright CLI.

## 7. QA / LLM Council — что исправлено и что отложено
Исправлено: видимый `:focus-visible` везде; `lang="ja"`; `aria-live` форма/ошибки; Esc закрывает меню; `body.menu-open{overflow:hidden}`; гард прошлых месяцев в календаре; сброс `dataset.date` после отправки; honeypot усилен; Safari-safe `clamp()` имени; постеры портфолио; абсолютный OG-URL + карточка 1200×630 + canonical + apple-touch-icon; `--faint` поднят до 70% для контраста; гард ресьюма шоурила; distinct aria-labels портфолио; paths-фильтр в deploy.yml.
**Отложено (по решению клиента):** правки копирайта (есть конкретные предложения редактора — спросить клиента), кейсы + визуал. **Намеренно оставлено:** катакана вермильоном.
**Не прогнано:** Lighthouse (нет Chrome) — замерить на проде/PageSpeed; цель ≥90.

## 8. Бронирование — состояние и план бэкенда
- Сейчас фронт: календарь (демо-занятость `busy` Set в main.js), форма (валидация имя/контакт, honeypot, состояния). Без бэкенда — **honest fallback**: формирует заявку, копирует в буфер, ведёт в Telegram @nickmenshov. `ENDPOINT=''` в main.js.
- **Прайс на фронте (v2):** при выборе формата в `#f-format` показывается панель `#pricePanel` (модуль `pricing` в main.js, цены в `data-base/-hours/-ext` опций). Цена + продление подставляются в текст заявки в Telegram. При смене цен — правьте `data-*` у `<option>` в index.html (бэкенду цифры дублировать не нужно, источник истины — разметка).
- План (когда дадут креды CP3): **Cloudflare Worker** → POST заявки → Telegram Bot API `sendMessage` (нужны **BOT_TOKEN**, **CHAT_ID**) + создание черновика события в **Google Calendar** (service account ИЛИ секретная iCal для чтения занятости, кэш 10–15 мин). Секреты — ТОЛЬКО в env воркера. Антиспам: honeypot (есть) + rate limit на воркере. После — прописать URL в `ENDPOINT` и тянуть занятость в календарь вместо `busy`.

## 9. Что нужно от клиента (TODO для продолжения)
1. **Видео для раздела «Обо мне»** (по просьбе клиента): можно со звуком, либо фоном с субтитрами. Сейчас в «Обо мне» видео нет — место под него готовится.
2. Материалы/правки **кейсов** раздела «Саунд-дирекция» (за кнопкой «Смотреть кейсы»): названия проектов, цифры, медиа. Заполняем «в самом конце, как упакуем кейсы».
3. **Фото** для галереи «Портфолио» (галерея уже принимает `<img>`) + портрет «Обо мне»; **миксы** для плеера (CP2). На Я.Диске фото НЕТ.
4. Креды бэкенда: **BOT_TOKEN, CHAT_ID, доступ к Google Calendar** (CP3).
5. Решение по **копирайту** (предложения редактора в логе — не применены).
6. **«Ок» на деплой** + подтвердить репозиторий/домен.

## 10. Деплой (когда разрешат)
- Репо (предположительно) **github.com/nikumen/nick-menshov-landing**, Pages: Settings → Pages → Source: **GitHub Actions**. URL: `https://nikumen.github.io/nick-menshov-landing/`.
- Если сменится владелец/репо/домен — поправить абсолютные `og:image`/`og:url`/`canonical` в `site/index.html` (помечено комментарием `BASE_URL`).
- `.gitignore` исключает тяжёлое исходное видео (`concept/media/IMG_*`, `long_*`); коммитятся только `site/media/reel*.mp4` (~13 МБ).
- После деплоя: проверить прод Playwright/PageSpeed, сквозной тест формы (после бэкенда).

## 11. Контакты артиста / ссылки
TG **@nickmenshov** · канал **@soundslikenick** · IG **@nickmenshov** · тел **+7 910 572‑25‑08**.
Top100 Awards (жюри): https://top100awards.ru/jury/menshov · Top15 Moscow (участник): https://top15moscow.ru/djs/nickmenshov · портфолио-видео Я.Диск: https://disk.yandex.ru/d/HwXuAkT3v2b2Sw
